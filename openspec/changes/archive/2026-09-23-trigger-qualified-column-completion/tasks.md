## 1. Completion interaction

- [x] 1.1 Register the dot trigger and qualified identifier edits; verify actual nvim-cmp automatic triggering, partial filtering, default Insert acceptance, midword replacement, and UTF-8 positions against the companion server.
- [x] 1.2 Add the reusable nvim-cmp test dependency and integration coverage; run focused checks and the full client suite with isolated real-server Sessions and child cleanup.

## 2. Documentation and verification

- [x] 2.1 Update README, architecture, roadmap, and development instructions; verify ownership links, compatibility guidance, and examples against the tested behavior.
- [x] 2.2 Record shared server/client evidence and remaining limits, pass strict OpenSpec and whitespace validation, and synchronize the verified contract for completion.
