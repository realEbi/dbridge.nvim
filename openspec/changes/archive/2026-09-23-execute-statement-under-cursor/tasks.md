## 1. Statement selection and execution

- [x] 1.1 Implement byte-based lexical statement selection and verify quotes, comments, delimiters, multibyte offsets, trigger bodies, empty input, and incomplete input with behavior tests.
- [x] 1.2 Add the command and query-editor mapping through the existing execution flow; verify only the chosen statement executes in real SQLite/DuckDB Sessions and existing whole-buffer execution remains available.

## 2. Documentation and completion

- [x] 2.1 Update README, architecture, roadmap, and development coverage documentation; verify examples and references against implemented behavior.
- [x] 2.2 Run the full client suite, record evidence/limits, validate OpenSpec and whitespace, then synchronize and archive the verified contract.
